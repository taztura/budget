{
  "name": "budget",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd src/fe && npm install && npm run build",
    "start": "node src/be/server.js",
    "dev":   "node --watch src/be/server.js"
  },
  "dependencies": {
    "cors":    "^2.8.5",
    "express": "^4.19.2",
    "sql.js":  "^1.11.0"
  }
}
